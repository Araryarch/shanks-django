meta {
  name: List Posts
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/posts?page=1&limit=10
  body: none
  auth: none
}

params:query {
  page: 1
  limit: 10
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });
  
  test("should return posts array", function() {
    expect(res.body.posts).to.be.an('array');
  });
}
